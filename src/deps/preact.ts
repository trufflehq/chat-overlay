// don't import from here for now. just import 'react' directly
// (it'll use preact/compat)

// export * from 'react';
// this is what we want to use here (vite will convert react to preact/compat)
// but typescript doesn't like re-exporting * from react

// https://github.com/microsoft/TypeScript/issues/51923
